import { SxProps, Theme } from "@mui/material";

export const styles: { [key: string]: SxProps<Theme> } = {
  boxContainerStyles:{
    maxWidth: 1200,
    margin: "0 auto",
  },
  sliderItemStyles:{
    padding: "0 10px",
    width: "100%",
  }
}