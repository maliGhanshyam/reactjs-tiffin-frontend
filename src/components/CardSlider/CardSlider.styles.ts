import { SxProps, Theme } from "@mui/material";

export const boxContainerStyles: SxProps<Theme> = {
  maxWidth: 1200,
  margin: "0 auto",
};

export const sliderItemStyles: SxProps<Theme> = {
  padding: "0 10px",
  width: "100%",
};
