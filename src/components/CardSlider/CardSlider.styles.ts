import { SxProps, Theme } from "@mui/material";

export const styles: { [key: string]: SxProps<Theme> } = {
  boxContainerStyles: {
    padding: {sm:"0 50px",xs:"0px 25px"},
    margin: "auto",
  },
  sliderItemStyles: {
    padding: "0 10px",
    width: "100%",
  },
};
